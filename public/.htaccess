RewriteEngine On

# Block direct access to dotfiles
RewriteRule "^\." - [F]

# Pass everything that's not a real file/dir through index.php
RewriteCond %{REQUEST_FILENAME} !-f
RewriteCond %{REQUEST_FILENAME} !-d
RewriteRule ^ index.php [QSA,L]
